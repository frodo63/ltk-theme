{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region div.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @ingroup templates
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}


    <div{{ attributes.addClass(classes) }}>{{ content }}</div>
    <div class="side-menu--wrap"><div class="side-menu-burger-link">
            <div class="side-menu-burger--icon">
                <span></span>
                <span></span>
                <span></span>
            </div>
            <div class="side-menu-burger--name no-copy hidden-xs">МЕНЮ</div>
        </div></div>

{% endif %}


{#
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}
{% if content %}
    <div class="side-menu--wrap">
      <div class="side-menu-burger-link">
          <div class="side-menu-burger--icon">
              <span></span>
              <span></span>
              <span></span>
          </div>
          <div class="side-menu-burger--name no-copy">ПРОДУКЦИЯ</div>
      </div>
      <div{{ attributes.addClass(classes) }}>
          {{ content }}
      </div>
    </div>
{% endif %}
#}